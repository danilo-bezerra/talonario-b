import { VStack, Heading} from 'native-base';

export function Multas () {

  return (
    <VStack flex={1} alignItems="flex-start" bg="gray.600" px={8} pt={15}>
    <Heading>Multas</Heading>
    </VStack>
  )
}